import { PictureAnalysisDirective } from './picture-analysis-directive';

describe('PictureAnalysisDirective', () => {
  it('should create an instance', () => {
    const directive = new PictureAnalysisDirective();
    expect(directive).toBeTruthy();
  });
});
