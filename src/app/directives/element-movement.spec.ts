import { ElementMovement } from './element-movement';

describe('ElementMovement', () => {
  it('should create an instance', () => {
    const directive = new ElementMovement();
    expect(directive).toBeTruthy();
  });
});
