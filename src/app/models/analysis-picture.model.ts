export interface PictureAnalysisValue{
    clickX:  number,
    clickY:  number,
    currentWidth: number,
    currentHeight: number,
    naturalWidth: number,
    naturalHeight: number,
    percentCurrentFromNaturalWidth: number,
    percentCurrentFromNaturalHeight: number
}